spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:${DB_URL:jdbc:postgresql://localhost:5432/discodeit}}
    username: ${SPRING_DATASOURCE_USERNAME:${DB_USERNAME:discodeit_user}}
    password: ${SPRING_DATASOURCE_PASSWORD:${DB_PASSWORD:discodeit1234}}

server:
  port: ${SERVER_PORT:8080}
